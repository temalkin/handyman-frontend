// Single source of truth for all images used across the website
// For files in /public, use root path string
// For external images, use full URL string
// For src/assets files, import and assign to keys

// Deprecated: use `src/config/images.js` instead
export { IMAGES } from '../config/images';